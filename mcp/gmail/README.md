# Gmail MCP Server

Gmail integration allowing reading, drafting, and sending emails.

## Setup and Configuration

This server runs via NPX:
```sh
npx -y @modelcontextprotocol/server-gmail
```

To configure, register it with your MCP client (e.g., Claude Desktop).
