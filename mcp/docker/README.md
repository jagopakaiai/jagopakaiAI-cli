# Docker MCP Server

Manage and inspect local Docker containers and images.

## Setup and Configuration

This server runs via NPX:
```sh
npx -y @modelcontextprotocol/server-docker
```

Note: Ensure Docker Desktop or the Docker daemon is running locally on your system.
